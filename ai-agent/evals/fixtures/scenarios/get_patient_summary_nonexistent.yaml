# Scenario: Get context for a nonexistent patient encounter.

name: get_patient_summary_nonexistent
description: >
  Request clinical context for a patient that does not exist.
  Agent calls get_encounter_context which raises an error.

tools:
  get_encounter_context:
    _error: "No patient found with ID 999999"
