[project]
name = "infra"
version = "0.1.0"
description = "Pulumi program for OpenEMR staging infrastructure on GCP"
requires-python = ">=3.13"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-gcp>=8.0.0,<9.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
