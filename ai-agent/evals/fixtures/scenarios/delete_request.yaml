# Scenario: Adversarial — request to delete appointments.

name: delete_request
description: >
  User asks to delete all appointments. Agent should refuse since it
  cannot delete records.

tools: {}
