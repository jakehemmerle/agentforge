diff --git a/docker/development-easy/docker-compose.yml b/docker/development-easy/docker-compose.yml
index 72e4e1eb8..639124625 100644
--- a/docker/development-easy/docker-compose.yml
+++ b/docker/development-easy/docker-compose.yml
@@ -91,6 +91,8 @@ services:
       OPENEMR_SETTING_SMTP_Auth: 'TRUE'
       OPENEMR_SETTING_practice_return_email_path: 'noreply@openemr.local'
       OPENEMR_SETTING_Patient_Reminder_Sender_Name: 'OpenEMR Reminders'
+      AI_AGENT_URL: http://localhost:8350
+      AI_AGENT_API_KEY: ${AI_AGENT_API_KEY:-}
     depends_on:
       mysql:
         condition: service_healthy
@@ -181,6 +183,31 @@ services:
       interval: 30s
       timeout: 5s
       retries: 3
+  ai-agent:
+    build: ../../ai-agent
+    ports:
+    - 8350:8350
+    volumes:
+    - ../../ai-agent:/app:rw
+    environment:
+      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
+      LANGSMITH_TRACING: "true"
+      LANGSMITH_API_KEY: ${LANGSMITH_API_KEY:-}
+      LANGSMITH_PROJECT: openemr-agent
+      OPENEMR_BASE_URL: http://openemr:80
+      OPENEMR_CLIENT_ID: ${OPENEMR_CLIENT_ID:-}
+      OPENEMR_CLIENT_SECRET: ${OPENEMR_CLIENT_SECRET:-}
+      API_KEY: ${AI_AGENT_API_KEY:-}
+    depends_on:
+      openemr:
+        condition: service_healthy
+    command: ["uv", "run", "uvicorn", "ai_agent.server:app", "--host", "0.0.0.0", "--port", "8350", "--reload", "--reload-exclude", ".venv"]
+    healthcheck:
+      test: ["CMD", "curl", "-f", "http://localhost:8350/health"]
+      start_period: 30s
+      interval: 30s
+      timeout: 5s
+      retries: 3
 volumes:
   databasevolume: {}
   assetvolume: {}
